$("table tbody").empty();
$("table tbody").html('<%= escape_javascript render(@teams) %>');

$('.pagination').parent().html('<%= escape_javascript paginate(@teams, remote: true).to_s %>');
$('.page-info').html('<%= escape_javascript page_entries_info(@teams).to_s %>');
