/ Navigation
%nav.navbar.navbar-inverse.navbar-fixed-top{:role => "navigation"}
  .container
    / Brand and toggle get grouped for better mobile display
    .navbar-header
      %button.navbar-toggle{type: "button", data: {target: ".navbar-ex1-collapse", toggle: "collapse"}}
        %span.sr-only Toggle navigation
        %span.icon-bar
        %span.icon-bar
        %span.icon-bar
      %a.navbar-brand{href: "/"} RecTime
    / Top Menu Items
    %ul.nav.navbar-right.top-nav
      - if user_signed_in?
        %li.dropdown
          %a.dropdown-toggle{"data-toggle" => "dropdown", :href => "#"}
            = fa_icon 'user', text: current_user.username
            %b.caret
          %ul.dropdown-menu
            %li= link_to 'Profile', current_user
            %li.divider
            %li= link_to 'Log out', destroy_user_session_path, method: :delete
      - else
        %li= link_to fa_icon('user-plus', text: 'SIGN UP'), new_user_registration_path
        %li= link_to fa_icon('sign-in', text: 'LOG IN'), new_user_session_path
  / Sidebar Menu Items - These collapse to the responsive navigation menu on small screens
  / #bs-sidebar-navbar-collapse-1.collapse.navbar-collapse.navbar-ex1-collapse
  /   %ul.nav.navbar-nav.side-nav
  /     %li{class: ('active' if current_page?(dashboard_path))}
  /       %a{href: dashboard_path}
  /         = fa_icon('dashboard lg', style: 'padding-right: 15px;')
  /         %span.pull-right Dashboard
  /     %li{class: ('active' if controller?('experiments'))}
  /       %a{href: experiment_dash_path}
  /         = fa_icon('bar-chart-o lg', style: 'padding-right: 15px;')
  /         %span.pull-right Experiments
  /     %li{class: ('active' if controller?('organisms', 'strains'))}
  /       %a{href: organisms_path}
  /         = fa_icon('flask lg', style: 'padding-right: 15px;')
  /         %span.pull-right Organisms
  /     %li{class: ('active' if controller?('media'))}
  /       %a{href: media_path}
  /         = fa_icon('table lg', style: 'padding-right: 15px;')
  /         %span.pull-right Media
  /     %li{class: ('active' if controller?('tools'))}
  /       %a{href: equipment_index_path}
  /         = fa_icon('thermometer-quarter lg', style: 'padding-left: 5px;')
  /         %span.pull-right Equipment
  /     %li{class: ('active' if controller?('analyses'))}
  /       %a{href: analyses_path}
  /         = fa_icon('window-restore lg', style: 'padding-right: 15px;')
  /         %span.pull-right Analysis
