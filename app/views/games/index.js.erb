$("table tbody").empty();
$("table tbody").html('<%= escape_javascript render(@games) %>');

$('.pagination').parent().html('<%= escape_javascript paginate(@games, remote: true).to_s %>');
$('.page-info').html('<%= escape_javascript page_entries_info(@games).to_s %>');
